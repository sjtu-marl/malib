training:
  interface:
    type:
  config:
env_description:
  id:
  possible_agents:
algorithms:
rewards:
rollout:
  type:
  stopper:
  mode:
  fragment_length:
  num_episodes:
evaluation:
dataset_config:
